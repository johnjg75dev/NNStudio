{
  "name": "nnstudio-frontend",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "React front-end for NNStudio — the neural network training studio.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node scripts/check-undefined.mjs",
    "smoke": "node scripts/smoke-render.mjs",
    "verify": "npm run check && npm run smoke && npm run build",
    "prebuild": "node scripts/check-undefined.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@babel/parser": "^7.29.8",
    "@babel/traverse": "^7.29.8",
    "@vitejs/plugin-react": "^4.3.1",
    "esbuild": "^0.21.5",
    "vite": "^5.4.8"
  }
}
